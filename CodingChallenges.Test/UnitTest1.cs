using System.Collections.Generic;
using System.Linq;
using Xunit;

namespace CodingChallenges.Test;

public class UnitTest1
{
    [Fact]
    public void Test1()
    {
        var uniquePairs = new List<int[]>();
        uniquePairs.Any(c => c[0] == 1 && c[1] == 2);

        var arr = new[] {2, 3, 5};
        
        arr.Where(x => x + )
    }
}